# Core API dependencies
fastapi==0.104.0
uvicorn==0.23.2
websockets>=15.0
python-dotenv==1.0.0
httpx==0.25.0
python-multipart==0.0.20

# NOTE: The main prompt-ops package should be installed in editable mode from the repo root:
#   cd /path/to/prompt-ops && pip install -e .
#
# This ensures you're using the local development version of the package.
# The package includes all core dependencies (dspy, litellm, scipy, etc.)

# Dataset analysis dependencies
pandas>=1.5.0
PyYAML>=6.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
